<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab3tp1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab3TP1">
        <activity
            android:name=".Ui.Registro.RegistroActivityViewModel"
            android:exported="true"
            android:label="@string/title_activity_registro_view_model"
            android:theme="@style/Theme.Lab3TP1.NoActionBar" />
        <activity
            android:name=".Ui.Registro.RegistroActivity"
            android:exported="true" />
        <activity
            android:name=".Ui.login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>